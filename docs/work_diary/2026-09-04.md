# 2026-09-04 — Repo scaffold, first contact with the robot

## Done

- Repository structure created: colcon workspace layout, `perception/` vs
  `src/tita_perception/` split, requirements files (workstation / dev /
  Jetson), `.gitignore` and `.gitattributes` with LFS patterns, `tita.repos`
  for upstream sources. No code.
- Researched the platform: Jetson Orin NX, JetPack 6.0, ROS 2 Humble;
  vendor SDK entry point `LocomotionCmd` on `command/user/command`.
- Found the official manual (Chinese only):
  <https://tita-development-manual-uc.readthedocs.io/zh-cn/latest/>
- SSH access established. Three addresses depending on connection
  (USB-C / AP hotspot / client WiFi) — see
  [`../hardware/robot-access.md`](../hardware/robot-access.md).
- Full topic list captured from the robot → `hardware/topic-map.md`.
  Key findings: namespace `/tita3037072`; `command/manager/cmd_twist` is a
  plain `Twist`; a `tower_msgs_bridge_node` runs; `system/status` is the
  diagnostics topic.
- First bag recorded (`indoor_run_01`, 14.5 s, stationary). Camera measured at
  ~42 Hz live, ~32 Hz when recording; CameraInfo only ~9.4 Hz.
- **`camera/point_cloud` has no publisher** — `obstacle_detector_node` waits
  for it. Stereo depth is not running on the robot as delivered.
- MCAP storage plugin and `tmux` found missing on the robot.
- Docs written: status, risks, object-detection walkthrough, ROS 2 command
  reference, Foxglove workflow.

## Decisions

- Thesis goal fixed as **autonomous driving with mapping while driving**;
  the balancing-platform angle is a secondary observation, not the objective.
- YOLOX (Apache-2.0) over Ultralytics (AGPL) as the detector.
- RTAB-Map stereo mode as the leading SLAM candidate, `slam_toolbox` as a
  comparison baseline.

## Stopped at

Bags copied to the laptop. Detection pipeline not yet started.

## Note

`hardware/robot-access.md` and `hardware/topic-map.md` were written this day
but **not committed** — they were lost and had to be restored on 2026-09-15.
Commit at the end of every session.
