"""tita_perception - runtime object detection for the TITA robot.

Runtime only: loads an exported artifact from ``models/`` and turns camera
images into detections. No training code lives here (see ``perception/``).
"""

__version__ = "0.1.0"
