# ---------------------------------------------------------------------------
# On-robot dependencies - Jetson Orin NX 16GB, JetPack 6.0 (L4T r36.3),
# Ubuntu 22.04, Python 3.10, ROS 2 Humble.
#
# DO NOT `pip install -r requirements.txt` on the Jetson. PyPI has no aarch64
# CUDA wheels; you would silently get a CPU-only torch and lose ~100 TOPS.
#
#   pip install --no-cache-dir \
#       --extra-index-url https://pypi.jetson-ai-lab.dev/jp6/cu122 \
#       -r requirements-jetson.txt
#
# Already provided by JetPack - never pip-install these:
#   cv2 (CUDA-enabled OpenCV), tensorrt, pycuda, cuda-python
# Already provided by apt (/opt/ros/humble) - never pip-install these:
#   rclpy, cv_bridge, tf2_ros, sensor_msgs_py
# ---------------------------------------------------------------------------

numpy>=1.24,<2.0
scipy>=1.10,<2.0
pyyaml>=6.0
loguru>=0.7
tqdm>=4.66

# CUDA builds for aarch64 - resolved from the jetson-ai-lab index above.
torch==2.3.0
torchvision==0.18.0
onnx>=1.15
onnxruntime-gpu>=1.17

# TensorRT engine building / benchmarking helpers
polygraphy>=0.49
pycuda>=2024.1               # only if JetPack's build is unavailable
